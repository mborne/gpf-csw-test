[project]
name = "gpf-csw-test"
version = "0.1.0"
description = "Functional tests reproducing the AnyText/wildcard bug on the Geoplateforme CSW service"
requires-python = ">=3.10"
dependencies = []

[tool.uv]
package = false

[dependency-groups]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
